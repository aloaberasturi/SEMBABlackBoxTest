#! /usr/bin/env python3
class Filters:
    def __init__(self,size,keyWords,comparison = "none"):
        self.size = size
        self.keyWords = keyWords
        self.comparison = comparison
        